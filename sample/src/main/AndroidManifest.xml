<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
    >

    <uses-permission android:name="Manifest.permission.BLUETOOTH" />
    <uses-permission android:name="Manifest.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="Manifest.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="Manifest.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".example1_scanning.ScanActivity"
            android:label="@string/title_example1"/>

        <!--
  <receiver android:name="com.polidea.rxandroidble2.sample.example1a_background_scanning.ScanReceiver"/>
        <activity
            android:name="com.polidea.rxandroidble2.sample.example1a_background_scanning.BackgroundScanActivity"
            android:label="@string/title_example1">
        </activity>
        -->
        <activity
            android:name=".example3_discovery.ServiceDiscoveryExampleActivity"
            android:label="@string/title_example3" />
        <activity
            android:name=".example4_characteristic.CharacteristicOperationExampleActivity"
            android:label="@string/title_example4" />
        <activity android:name=".DeviceActivity" />
    </application>

</manifest>